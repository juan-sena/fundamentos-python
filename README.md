Seccion 1
-de la seccion 1 en el ejercico print cuando le quito las comillas para imprimir me votaron un error de sistasis de caracter invalido ya que si deseo imprimir un texto en pantalla deberia ir con comillas de lo contrario si no las coloco python lo tomaria como una variale y como no existe esa varible me da error 

-Si le quitamos los parentesis no dara error de sistasis pero esta ve que nos faltan los parentisis ya que anteriormente print en python era una instruccion y se ponia escribir asi pero ahora print es una funcion de python lo cual debe escribirse con parentesis para recibir un argumento

-En el siguiente ejercicio pasa que nos da un error de sistasis ya que al no cerrar debidamente las comillas python lo interpreta como codigo y no como una cadena de texto para imprimir 

-Si le quitamos un parentesis nos dara error ya que print en el actual python es una funcion lo cual requiera que cierren parentecis, esto hace que python impide que lo interprete la intrucción 

-Si cambiamos print a Print nos dara un error de name "Print" is not defined, Esto ocurre por que python es un lenguaje sensible a mayusculas y minusculas.Es decir, print y Print son considerados identificadores completamente diferentes, en este caso print es una funcion y Print python lo considera como una variable 

-El uso de comillas simples y dobles es válido en Python, pero al encerrar toda la expresión en comillas simples, se convierte en texto literal, evitando que se ejecute la concatenación. Por ello, es importante usar correctamente las comillas para no alterar el comportamiento del programa.